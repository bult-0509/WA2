# 仅用于本机开发；复制为 .env 后替换本地密码。
DATABASE_URL=postgresql://wemove:replace_local_password@127.0.0.1:5432/wemove
POSTGRES_USER=wemove
POSTGRES_PASSWORD=replace_local_password
POSTGRES_DB=wemove
API_PORT=3101
# db:setup 在缺失时生成随机 APP_KEY，用于后台 MFA 加密。
WEB_ORIGIN=http://127.0.0.1:3100
